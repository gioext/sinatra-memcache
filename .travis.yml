sudo: false
cache: bundler
language: ruby

rvm:
  - 2.1.10
  - 2.2.5
  - 2.3.1
  - ruby-head

matrix:
  allow_failures:
    - rvm: ruby-head
  fast_finish: true

before_install: gem update --remote bundler
script: bundle exec rake
